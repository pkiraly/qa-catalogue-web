{% if attribute(tag.subfields, code) is iterable %}
  {% for subfield in attribute(tag.subfields, code) %}
    {% include 'search/link.html.twig' with {value:subfield, solr_field:solr_field}
       %}{% if not loop.last %}, {% endif %}
  {% endfor %}
{% else %}
  {% include 'search/link.html.twig' with {value:attribute(tag.subfields, code), solr_field:solr_field} %}
{% endif %}
